spring:
    datasource:
        url: jdbc:h2:mem:testdb
        initialization-mode: always
    h2:
        console:
            enabled: true
            path: /h2
    jpa:
        show-sql: true
        generate-ddl: true
        hibernate:
            ddl-auto: create-drop
